const BluetoothIcon = Widget.Icon({
    icon: 'bluetooth-active-symbolic',
    size: 20,
})
const GoNextIcon = Widget.Icon({
    icon: 'go-next-symbolic',
    size: 20,
})

export { BluetoothIcon, GoNextIcon }
